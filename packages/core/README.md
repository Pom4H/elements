# @pom4h/elements-core

Runtime, registry, composition, motion, scene and routing primitives for semantic SVG custom elements.

```bash
npm install @pom4h/elements-core
```

Definitions and manifest helpers are server-safe. Browser registration and DOM runtime entrypoints are explicit exports.

See the repository README for architecture, element contracts and examples.
